# Root dev install — installs shared package + all app dependencies
# Usage: pip install -r requirements-dev.txt
-e ./nivesh-shared
-r ./nivesh-server/requirements.txt
# -r ./nivesh-client/requirements.txt  # Uncomment after Phase 4 adds client requirements.txt
